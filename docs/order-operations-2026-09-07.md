# Recovery and durable order operations

Extends production baseline `80740061` without changing checkout/payment creation. Recovery remains **OFF**. No customer recovery email was sent during implementation or tests.

At September 7, 16:14 UTC: `c259b020` is a 160-hour draft with an incomplete Stripe intent, but a newer matching-customer order has a live $74.99 authorization: **do not send**. `390082fe` is a 46-hour draft; Stripe requires a payment method, with zero received/capturable and no newer same-email/user order: **appropriate after founder approval**, subject to the live recheck at delivery.

The existing Resend sender now supports recovery with no discount, email-bound seven-day capabilities, live Stripe and replacement checks, recipient suppression, unique order/touch claims, stable provider idempotency keys/body, and persisted outcome. Schedule: every 15 minutes; first touch after one hour, second after 24 hours; delayed runs skip the early touch. Minimum 20 hours between recipient touches. Ambiguous sends retry with the same key for less than 23 hours, then stop for provider review and block another touch. Admin shows a warning for these cases. Do not reset an uncertain delivery without checking the provider.

Treat proactive reminders as commercial email: truthful existing sender/subject, commercial identification, valid postal address, and immediate opt-out without login. GET renders confirmation without changing preferences; POST (including one-click unsubscribe) persists an HMAC email suppression. Essential receipts/order updates continue. Opt-out links have no expiry; keep the existing recovery signing secret stable. See [FTC guidance](https://www.ftc.gov/business-guidance/resources/can-spam-act-compliance-guide-business) and [Resend's 24-hour idempotency limit](https://resend.com/docs/dashboard/emails/idempotency-keys).

**Activation action, only after founder approval and a valid mailing address:** authenticated admin POST `/api/admin/order-operations` with `{ "action": "enable_recovery", "confirmFounderApproval": true, "postalAddress": "<valid business mailing address>" }`. This records actor/time and enables the next scheduled check. Approval/address have not been supplied. Immediate stop: same endpoint with `{ "action": "disable_recovery" }`, or service-only `update public.order_operations_control set recovery_enabled=false where id`. Disabling prevents new submissions; it cannot recall a message already accepted by Resend. `RECOVERY_FORCE_DISABLED=true` is an additional deployment-level override.

The existing Supabase project runs `hl-order-operations` every 15 minutes through pg_cron/pg_net, with its dedicated bearer in encrypted Vault and Vercel `HL_OPERATIONS_SECRET`. Vercel Hobby only allows daily cron, so no plan upgrade/new provider was introduced. The schedule configuration RPC is service-only and installation does not start the job; provisioning sets the secret/schedule. Runner lease prevents overlap. `order_operations_control` persists last attempt/success/error; admin warns after an hour without success. Inspect `cron.job_run_details` and `net._http_response` for scheduler/HTTP failures. See [Supabase scheduling](https://supabase.com/docs/guides/functions/schedule-functions).

Paid/authorized orders become durable Needs Attention exceptions after 24 hours without status progress, ordered/backordered after 72 hours, shipped after 10 days. Live Stripe excludes fully refunded/cancelled/non-actionable payments; partial refunds remain actionable. Completed/delivered/cancelled fulfillment and explicit tests are excluded. Existing active alerts survive unrelated timestamps, restarts, and the ordinary order-list row limit. State changes resolve/reset aging. The order modal offers 24-hour acknowledgment; the alert remains visible. Existing founder email infrastructure gets at most one claimed notification per unchanged state; notification failure remains visible and is not blindly resent.

Focused mock tests cover kill switches, replacement/opt-out, concurrent dedup, immutable retries, uncertainty stop, partial refund, aging, restart dedup and resolution. Existing revenue/receipt/checkout tests are retained. Local PostgreSQL tests verify client denial, default-off/address constraint, claims/retries and runner lease. Production schema gate rejected missing tables before migration and passed afterward. Full tests, TypeScript, targeted lint and build passed. Security advisors show no new error-level finding; server-only tables intentionally have no client policies. The preexisting [leaked-password warning](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection) remains outside scope.

Routing evidence unchanged after targeted review: J&J direct pricing confirmed (submission automation unproven); CooperVision uses Nassau/OOGP pricing/route; Alcon and Bausch + Lomb submission routes remain **UNVERIFIED**. Manufacturer price lists do not establish ordering integration. Supplier submission/acceptance, tracking/backorder imports, Google Ads, historical email repairs and broad catalog work remain parked.
